## Learned User Preferences

- Phase 8 (iterate) complete ✅ — deferred: real async/await (bc coroutines), heap snapshot, export-list live bindings for `export const`/`export function`. Catch-up builtins shipped (dual tree+bc, registered in `realm.zig`, implemented in `runtime/builtins/*`): `globalThis`/`global`, `Object.getOwnPropertyDescriptors`, `String.prototype.replaceAll`, `Array.prototype.at`/`findLast`/`findLastIndex`, `Promise.allSettled`. Prior Phase 8 items remain: `**`, ES2020/2021 operators, ES2016–2019 builtins, native ESM, PTC, source maps/debugger stubs, bytecode snapshot. `zig build test` + `zig build differential` (94/94) green.
- Phase 9 JIT scaffold 🟡 — `src/jit/` (`JitCompiler` stub), `docs/JIT.md`, `zig build jit`, native count-loop kernel under `-Djit=true`. Hot-loop fast-forward (`loop_jit.zig tryFastForwardLoop`, hooked at bc_vm JMP back-edge) now GENERALIZED: header compare LT/LE/GT/GE; body step INC(+1)/DEC(-1) or LOAD_K+ADD/SUB(±const); termination guard (ascending⇒step>0, descending⇒step<0, else deopt). Native kernel used only for cmp==lt; zig kernel for the rest. Verified green: `zig build test`, differential 94/94. STAGED→DONE: (B) SMI/NaN-box Value rep — NOW LIVE (`enable_smi=true`) and green. `value.zig`: comptime `enable_smi` safety valve; low-bit tag (ptr aligned⇒bit0, SMI⇒bit1); `isSmi`/`smiValue`/`fromSmi`/`unbox()` superset accessor; `makeNumber` produces SMI when `smiFits` (integral, finite, ±2^53, EXCLUDING -0.0 to preserve the -0/+0 distinction). Migration done: all 235 `toPtr().*` read-dispatch sites → `unbox()`, plus ~34 direct `toPtr().number`/`.boolean` payload reads → `unbox().number/.boolean`. GC made SMI-safe at BOTH mark paths: `heap.zig markValue` and `gc.zig traceValue` early-return on `isSmi()`. Write sites use bound-local `toPtr()` (never SMI; untouched). VERIFIED green with SMI ON: `zig build test`, differential 94/94, conformance 70.71% with 0 unexpected flips. Safety valve intact: flip `enable_smi=false` to revert. NOTE: pre-existing fmt drift in the uncommitted working tree (unrelated files) — not from this work.
- Phase 9 value-rep EXTENDED (under same `enable_smi` flag, green): (1) IMMEDIATE singletons — null/true/false now allocation-free via free low-3-bit patterns {010,100,110} (`imm_null/imm_true/imm_false`), decoded in `unbox()`, produced by `makeNull`/`makeBool`; `isHeapPtr()` (`bits!=0 and bits&0b111==0`) added and BOTH GC guards switched to `!isHeapPtr()`. (2) SMI integer ARITH FAST-PATH (`value.smiArith(a,b,op)`) wired into bc_vm ADD/SUB/MUL: integer add/sub/mul with overflow→f64 fallback, defers ±2^53 overflow and MUL-to-0 (JS -0). Verified: `zig build test`, differential 94/94, conformance 70.71% 0 flips. DEFERRED: inline non-integer doubles — needs the canonical WebKit offset NaN-box (doubles-default, 48-bit pointers), which is incompatible with the current low-3-bit tag scheme + pervasive `bits==0==undefined` invariant + raw `toPtr()=@ptrFromInt`; that is its own representation migration (reads are centralized in `unbox()` but `bits==0` checks and `toPtr` casts are not).
- Phase 9 value-rep: FULL WebKit-style inline-double NaN-BOX now SHIPPED and LIVE (`enable_nanbox=true`, green) — supersedes the earlier deferral. `value.zig` constants from WebKit JSCJSValue.h: `NumberTag=0xfffe000000000000`, `DoubleEncodeOffset=1<<49`, immediates `nb_null=0x2`/`nb_false=0x6`/`nb_true=0x7`, `NotCellMask=NumberTag|0x2`. Encoding: int32=`NumberTag|u32` (isInt=`bits&NumberTag==NumberTag`), double=`@bitCast(d)+%offset` (NaN purified to canonical), number=`bits&NumberTag!=0`, cell/ptr=`bits!=0 and bits&NotCellMask==0`. KEY: kept `bits==0⇒undefined` (WebKit ValueEmpty=0) so ALL existing `bits==0` guards stay valid ⇒ minimal migration; reads already centralized in `unbox()` (decodes int32/double/immediate), `toPtr()` only for cells (numbers never flow there). `makeNumber` inlines everything (no alloc); `isSmi/smiValue/fromSmi` comptime-branch to int32 (`smi_min/max`=i32 range under nanbox). GC `isHeapPtr` uses NotCellMask. Mixed reps coexist (a boxed `.number` ptr has `bits&NumberTag==0` ⇒ still decodes via toPtr). Codec verified by a flag-independent round-trip+disjoint-classification test. VERIFIED green with nanbox ON: `zig build test`, differential 94/94, conformance 70.71% with 0 unexpected flips (incl -0 via double path, NaN canonical). Safety valves: `enable_nanbox=false` reverts to low-bit SMI+immediates; both still `=true`, nanbox takes precedence in each method. [SUPERSEDED by W7: the `enable_smi`/`enable_nanbox` safety-valve flags + the low-bit SMI/`imm_*` fallback paths were REMOVED — NaN-box is now the sole committed representation; `isSmi/smiValue/fromSmi/isHeapPtr/unbox/makeNull/makeBool/makeNumber` are single-branch nanbox-only.]
- W6 (Embedding API 1.0 stabilization) COMPLETE ✅ — chosen path: full persistence refactor + mark/gate freeze. (P1) PERSISTENT REALM: realm no longer rebuilt+torn-down per eval and `eval_arena.reset(.free_all)` no longer wipes script globals between evals ⇒ globals now survive across `Context.eval` calls (persist test added). (P2/P3) registerNativeFn/globalObject NO LONGER STUBS: native fn dispatch goes through `fn_ptr.invoke` (sets `g_active_native_data`); native throw convention = set `realm_mod.pending_exception` + return `error.JsException`. Added public Value constructors `makeNumber/makeString/makeBool/makeUndefined/makeNull` + `getProperty` so hosts can build/read values (NaN-boxed Value bridged root.Value↔val_mod.Value). (P4) API FREEZE in `src/root.zig` doc-header: STABLE (semver-frozen 1.0) = `version`, `Isolate`(init/deinit/newContext), `Context`(deinit/eval/registerNativeFn/globalObject/getProperty/make*/gc/gcStats/setLimits), `Value`(toI32/toF64/toString), result types, `valueToDisplayString`; EXPERIMENTAL (not semver-governed) = snapshot, dumpBytecode, sourceMap, debug, JIT surface, `_regex` (35 per-symbol markers). `examples/embed.zig` + `zig build example-embed` added, builds+runs in CI proving native-fn→cross-eval persistence→globalObject/getProperty chain end-to-end (`total=2`/`host sees total=2`). Debugger protocol was already substantially built (DebugSession, breakpoints, steps, hooks) ⇒ left as-is under EXPERIMENTAL `debug`, not cut. VERIFIED green: `zig build test` (0), differential 97/97, gc-stress PASS. NOTE: differential corpus grew 94→97.
- W7 (Tech-debt cleanup) — first pass DONE ✅. (1) DEAD CODE: deleted `src/regex/{vm,parser}.zig` stubs (git rm) — nothing imported them; the real regex engine is `regexp.zig` (exposed as `jsz._regex`). (2) VALUE-REP FLAG COLLAPSE: removed the `enable_smi`/`enable_nanbox` comptime safety-valve consts and the dead `imm_null/imm_true/imm_false` low-bit-immediate consts from `src/value/value.zig`; `isSmi/smiValue/fromSmi/isHeapPtr/unbox/makeNull/makeBool/makeNumber` are now single-branch NaN-box-only; `smi_min/max` are plain i32 bounds. Deleted the obsolete low-bit "SMI codec round-trips" test; dropped the `if(!enable_smi)return` guard from the immediate-singletons test (nanbox `nb_*` still satisfy its assertions). (3) LIVE-BINDING GAP: `export { a, b }` (non-aliased, non-`from`) now registers each local in `live_exports` (parser.zig `parseExportDecl` list branch, guarded `tmp==null && local==exported`), reusing existing `makeExportLive` (drops the snapshot `exports.a=a`, rewrites the `var a` init + use-sites to `exports.a`); added test `esm: export {a} list is a live binding`. RATIONALE for the rest of the gap list: `export const x=E` is a NON-GAP (const is immutable ⇒ snapshot always correct; live binding would conflict with const enforcement); `export function f(){}` reassignment-liveness is LOW VALUE (hoisted, rarely reassigned; `makeExportLive` only rewrites `var_decl`) — both deferred. MEMBER-POSITION TAIL CALLS (`return obj.m()`) — DONE ✅ (W7 follow-up): added `TAIL_METHOD_CALL` opcode (`opcodes.zig`, appended after YIELD to preserve ordinals; instrSize=4; disasm shares the METHOD_CALL case in `chunk.zig`). Compiler: `compileCall` is_method branch emits `TAIL_METHOD_CALL` when `tail`; `return_stmt` (`compiler.zig` ~1267) dropped the `member_expr` exclusion (kept the `is_strict && try_depth==0` guard ⇒ member calls in try/finally are still NOT tail calls). VM (`bc_vm.zig`, handler after METHOD_CALL): mirrors `TAIL_CALL` frame-reuse but this=R[base], callee=R[base+1], args at base+2; reuses the frame only for plain bc_function (guarded `!is_generator && !is_async`), else falls back to `doMethodCall` + return (which writes ret_dst inline for native/bound/getter/generator/async — no frame push). Test added: `tco: member-position tail recursion keeps stack O(1) in bc mode` (`return o.sum(...)`, 20000 deep, frames<=4, result 200010000). VERIFIED green: `zig build test` (0), differential 97/97. VERIFIED green: `zig build test` (0), differential 97/97. NOTE: conformance-delta + conformance-summary are STALE/unavailable in this environment — verification rests on test + differential.
- Phase 13 (Conformance Push to 95%) — three slices DONE ✅ (2026-06-07), all green (`rtk zig build test`, differential 124→133). (A) **ToPrimitive**: new shared `src/runtime/builtins/coercion.zig` `toPrimitive(arena,v,hint)->?Value` (Some=user hook via `Symbol.toPrimitive`/`valueOf`/`toString` through `function_proto.invokeCallback`; null=caller keeps its default ⇒ zero regression since only Date/Symbol protos define those). Wired into `bc_vm` `jsAdd` + guarded object slow-path on `SUB/MUL/DIV/MOD/EXP/NEG/INC/DEC/LT/LE/GT/GE`, plus `String()`/`Number()` natives + template literals (desugar to `+`). `active_sym_to_primitive` captured in realm; `Date.prototype[Symbol.toPrimitive]` added (string for default). CRITICAL reentrancy rule reused: any opcode handler that now calls user code must write its result via `self.frames.items[len-1].registers[rdst]` (re-fetch) — `invokeCallback` can reallocate `self.frames`; the arith IC `ac` ptr is into the stable func table so it's safe. Deferred: `==` ToPrimitive, bitwise object coercion. (B) **Array spread `[...iterable]`**: two appended opcodes `ARRAY_APPEND`/`ARRAY_SPREAD` (3 bytes); `compileArrayLiteral` keeps the static-index fast path when no spread else builds dynamically; `ARRAY_SPREAD` drives `__getIterator__`/`__iterStep__` (arrays/strings/Set/Map/generators/user `[Symbol.iterator]`), non-iterable ⇒ catchable TypeError via new `raisePendingException` helper. Deferred: call-arg spread `f(...args)` (static-`nargs` CALL ABI). (C) **Proxy get/set traps**: `new Proxy(t,h)` = JsObject `internal_kind==.proxy`; target+handler stored as symbol-keyed own props under 2 private symbols (`active_sym_proxy_target/handler`) so GC (traces `sym_props`, NOT `internal_slot`) keeps them alive + string-enum hides them. Dispatch at top of `getProp/setProp/getPropSym/setPropSym`; forwards to target when trap absent. **Fixed a PRE-EXISTING bug**: throwing accessor getter/setter (and now Proxy traps) inside try/catch ⇒ "Uncaught out of memory" because GET_PROP/GET_PROP_DYN/SET_PROP/SET_PROP_DYN did `try self.getProp/setProp` (propagated native `error.JsException` as a Zig error, bypassing the frame `try_stack`); now `catch |e| { if e!=JsException return e; if (try self.raisePendingException(..)) |oc| return oc; continue; }`. Deferred Proxy traps: `has`/`deleteProperty` (the `in`/`delete` operators are STUBS in this engine — `'a' in {a:1}` returns false, `delete` always true), `ownKeys`/`getOwnPropertyDescriptor` (enum not proxy-aware), `apply`/`construct` (callable proxies). GOTCHA: parser lacks method-shorthand `{f(){}}` and computed object-literal keys `{[s]:..}` — use `{f:function(){}}` and `o[s]=fn`.
- Phase 13 (cont.) — four more slices DONE ✅ (2026-06-07), all green (`rtk zig build test`, differential 133→138). (D) **Real `in` + `delete` operators**: were `LOAD_FALSE`/`LOAD_TRUE` stubs. New `IN` (op,Rdst,Rkey,Robj) + `DELETE_PROP` (op,Rdst,Robj,Rkey) opcodes (opcodes.zig+instrSize+chunk disasm). compiler.zig: `.in` (compileBinary) emits IN; `.delete_` emits DELETE_PROP for member_expr (computed via rkey, else LOAD_K name), else evaluate+LOAD_TRUE. bc_vm: IN handler (TypeError if obj non-object), DELETE_PROP handler; helpers `hasProperty` (proto-walk string+symbol keys, array `length`/index, Proxy `has` trap) + `deleteProperty` (Proxy `deleteProperty` trap, obj.deleteOwn/deleteOwnSym, non-object⇒true). FIXED pre-existing leak: `object.zig deleteOwn` rebuilt slots/attrs without freeing old arrays (`old_slots.deinit(self.arena)`) — only reachable once `delete` worked. (E) **Proxy has+deleteProperty traps**: dispatched from the new in/delete VM helpers via `proxy_mod.trap(handler,name)`, forwarding to target when absent. (F) **`==` ToPrimitive + bitwise object coercion**: `BcVm.abstractEqual(x,y)` — when exactly one side is object (other not null/undef) ToPrimitive(default) it (fallback to valueToString primitive) then `jsAbstractEqual`; EQ/NEQ guard `isObjectOperand`→abstractEqual else pure fast path. `toInt32Coerced`/`toUint32Coerced` + object slow-path on BIT_AND/OR/XOR/SHL/SHR/USHR/BIT_NOT. (G) **Call-arg spread `f(...args)`**: new `CALL_SPREAD` opcode (op,Rcallee,Rthis,Rargs,Rdst=5); compileCall detects `.spread_expr` arg → compileCallSpread builds args array (NEW_ARRAY+ARRAY_APPEND/ARRAY_SPREAD), compiles callee+this (member⇒this=object via GET_PROP[_DYN]; plain⇒LOAD_UNDEF), rdst=lowest slot reused (operands need NOT be contiguous); VM flattens array → `function_proto.invokeCallback(this,callee,slice)` → re-fetch frame. CRITICAL (reused rule): every handler that calls user code (IN/DELETE_PROP/EQ/NEQ/bitwise/CALL_SPREAD) writes its result via `self.frames.items[len-1].registers[rdst]` (re-fetch) since invokeCallback can realloc frames; uses `raisePendingException` to route native error.JsException into the frame try_stack. PITFALL I hit twice: when strReplace-ing the DELETE_PROP/CALL_SPREAD handlers I dropped the `// Phase 4d\n.GET_KEYS => {` opener (oldStr included it, newStr didn't) → "expected '}' found 'const'"; re-add the case opener. PRE-EXISTING gaps confirmed out-of-scope (broken for normal calls too): `arguments` object (returns undefined), rest params `...xs`; `Object.prototype` sparse so `'toString' in {}`=false vs Node true — avoid in corpus. REMAINING Phase 13: Proxy ownKeys/getOwnPropertyDescriptor/apply/construct, RegExp /u + property escapes + lookarounds, Intl, vendor test262 corpus.
- Phase 13 (cont. 2) — Proxy completion + RegExp flags DONE ✅ (2026-06-07), all green (`rtk zig build test`, differential 138→143, gc-stress PASS). (H) **Proxy apply+construct traps**: bc_vm helpers arrayFromSlice + proxyApply (handler.apply(target,this,argsArr); no trap⇒invokeCallback forward) + proxyConstruct (handler.construct(target,argsArr,newTarget); result must be object else TypeError; no trap⇒constructFromArgs forward). Wired into doCall `.object` branch (proxy→proxyApply, top, before bound_function), doConstruct `.object` branch (proxy→proxyConstruct, newTarget=callee), constructFromArgs `.object` branch. `new Proxy(...)` ctor itself is a plain object (internal_kind=.none) so unaffected. (I) **Proxy ownKeys+getOwnPropertyDescriptor traps**: proxy.zig proxyOwnKeys (ownKeys trap→array elems; no trap→target ownKeys strings) + proxyGetOwnPropertyDescriptor (gOPD trap or null⇒forward). object_methods.zig nativeObjectKeys/getOwnPropertyNames/getOwnPropertyDescriptor + bc_vm GET_KEYS (for-in) dispatch them (re-fetch frame after — trap runs user code). SIMPLIFICATION: Object.keys doesn't re-check per-key enumerable via gOPD. Proxy now has all 8 common traps. (J) **RegExp s/y/u flags**: compileRegex was throwing InvalidPattern on any non-igm flag. Flags struct +dotall/sticky/unicode; `.dot` respects dotall; new named MatchResult struct + findMatch (sticky⇒matchAt at lastIndex, else matchAnywhere); test/exec use use_li=global||sticky; dotAll/sticky/unicode props exposed. GOTCHA: Zig anon-struct return types are DISTINCT — findMatch returning matchAnywhere's anon struct failed; named MatchResult fixed it. DEFERRED (large, documented): full /u code-point semantics (byte-oriented engine: CharClass [256]bool, byte-advance matcher, non-ASCII \u truncates, \u{...} unparsed, . = 1 byte), \p{} property escapes, lookbehind; Intl; test262 corpus vendoring; Proxy invariant checks. Tests: +5 corpus (proxy_apply_construct, proxy_ownkeys, regex_dotall/sticky/unicode_flag) → differential 133→143; +10 integration tests.
- Phase 13 (cont. 3) — Intl + RegExp lookbehind/property-escapes DONE ✅ (2026-06-07), all green (`rtk zig build test`, differential 145→147). (K) **Intl** (new src/runtime/builtins/intl.zig, en-US, no ICU): NumberFormat (decimal/currency/percent + grouping + min/max fraction digits via integer math: scale=10^maxFrac, round-half-away, group commas, trim trailing zeros to minFrac; currency symbols $/€/£/¥, JPY=0 fraction; sign before symbol), DateTimeFormat (M/D/YYYY via date.zig msToFieldsUtc — UTC deterministic, NOT local TZ), Collator (std.mem.order → -1/0/1). realm.zig registers Intl namespace (NumberFormat/DateTimeFormat/Collator ctor objects, each __call__ + prototype carrying format/compare); instances store opts as own props (__intl_*), methods on prototype. date.zig: DateFields made pub + msToFieldsUtc + getDateMs added. CRITICAL: host Node default locale ≠ en-US (gives '1.234.567,891') → ALL Intl tests pass 'en-US' explicitly; Collator test SAME-CASE only (mixed-case = locale collation, differs from byte order). (L) **RegExp lookbehind**: look_behind RegexNode (mirror look_ahead); parseAtom (?<= /(?<! detect (peek '<' + src[pos+2] '='/'!'; named-group (?<name> ⇒ InvalidPattern); matchNode = zero-width, iterate j=pos..0 with throwaway caps, success if matchNode(inner,j)==pos (fixed-length exact; greedy variable-length may miss — documented). (M) **\p{} property escapes**: compileRegex now parses flags FIRST + threads flags.unicode into PatternParser.init; parseEscape 'p'/'P' (only under /u else identity literal) reads {Name}, fillPropertyClass ASCII approx (L/Letter/Alpha→a-zA-Z, Lu→A-Z, Ll→a-z, N/Nd/Number→0-9, Alnum, White_Space→\s); \P sets cc.negate. test262 corpus IS vendored at external/test262 (harness+tests present) but conformance-full HANGS (some tests loop; runner lacks per-test gas/time limits — wire setLimits before routine use). Tests: +6 corpus (intl_numberformat, intl_collator, regex_property_escape, regex_lookbehind) → differential 143→147; +10 integration. GOTCHA: '5'.replace(/(?<!\d)\d/g,'#')='#' (pos0 has no preceding digit) — my first unit-test expectation was wrong, engine correct. REMAINING Phase 13: full /u code-point engine rewrite (CharClass [256]bool→codepoint, \u{} astral, . =codepoint), non-ASCII property ranges, variable-length lookbehind, Intl extra locales, Proxy invariant checks, test262 gas-limited harness.
- Phase 13 test262 harness — per-test resource limits DONE ✅ (2026-06-07). `runOneTest` (src/test/test262_runner.zig) sets `ctx.setLimits(.{ time_ms=1000, mem_bytes=256*1024*1024, gas=0 })` before `ctx.eval` (Context default interp_mode=.bc enforces time/mem; limit breach → EvalResult.exception, never a crash). Detection: `if (result==.exception)` and message startsWith "interrupted:"/"out of memory"/"Uncaught out of memory" → return `.skip` (so a looping test can't masquerade as a passing negative test). This makes `conformance`/`conformance-summary`/`conformance-full` bounded (previously hung on looping corpus tests). REAL whitelist baseline now: **675 pass / 352 fail of 1027 = 65.72%** (built-ins 65.90, language 89.65, statements 84.37, expressions 58.49). Flips vs tests/test262_known_failing.txt: 86 unexpected fail, 20 unexpected pass (gains from Phase 13: switch/while/do-while/continue). known_failing list is STALE (corpus newly vendored) → `conformance-delta` gate needs a baseline refresh (deliberate follow-up; separate new-coverage fails from regressions). VERIFIED limit via CLI: `jsz --time-limit=500 -e "while(true){}"` → interrupted in 0.5s. test + differential still green (147/147). Runner consts: TEST262_PATH=external/test262/test, WHITELIST_PATH=tests/test262_whitelist.txt, KNOWN_FAILING_PATH=tests/test262_known_failing.txt.



## Learned Workspace Facts

- `jsz` is a Zig JavaScript runtime/compiler; Phases 0–8+ shipped; active track is Phase 9 (JIT scaffold); Phase 10 browser not started.
- Standard verification commands for this repo are `rtk zig build test` and `rtk zig build differential`; conformance: `rtk zig build conformance`, `conformance-delta`, `conformance-dashboard` (writes `docs/CONFORMANCE_DASHBOARD.md`), `conformance-summary`. Harness `src/test/test262_runner.zig`: per-category buckets, known-failing flip gate, whitelist dir-glob expansion; with corpus present ~1086 whitelisted tests @ ~70.7% pass, all categories ≥60%.
- Current practical ES2015 surface includes let/const/TDZ, arrows, for-of/spread iterators, class/super parser rewrites, default/rest/spread params, template interpolation, Map/Set/Weak collections, CommonJS shims, Promise microtasks/chaining/thenables, and tree-mode generators.
- Phase 7 (ES2015 highlights) verified complete: all deliverables work. Map/Set now support iterable constructors (`new Set([…])`, `new Map([[k,v]…])`) and a virtual `.size` accessor (handled like array `.length` in both `vm.zig` getProperty and `bc_vm.zig` getProp via `es2015_collections.collectionSize`; WeakMap/WeakSet intentionally have no `.size`). async/await is ES2017 and deliberately out of Phase 7 scope; generators remain tree-mode only; native ES modules deferred to Phase 8.
- Generator support is tree-mode focused; BC VM still lacks generators/yield/Phase 7 bytecode support.
- Tree-mode generator resume behavior is practical but not fully incremental; body re-materialization can re-run side effects between yields.

- Phase 6 (shapes + ICs) is complete and verified: `shape.zig` transitions + `ic.zig` mono→poly→mega are wired into `bc_vm` (per-site `ic_table`/`arith_ic_table`/`typeof_ic_table`/`instanceof_ic_table`). Bench (`zig build bench-phase6`) shows bc-vs-tree 1.6–1.8x. KNOWN LIMITATION: the `props` StringArrayHashMap on `JsObject` is still the authoritative store — GC marking (`gc/heap.zig`, `gc.zig`) and all enumeration (`Object.keys/values`, `JSON.stringify`, `for-in`) iterate `props` for values; the shape `slots` array is only a fast-read/IC cache, so writes must keep `props.put` in sync. Fully removing `props` requires making GC marking + enumeration shape-aware (a GC-touching refactor), deliberately deferred.
