# Wave 49b — Temporal All (built-ins/Temporal + intl402/Temporal)

You are working in a git worktree of JSZ, a JavaScript (ECMAScript) engine written in Zig.

## Context
- This is a **new branch** `feat/conformance-wave49b` based on `origin/main` (Wave 47c baseline)
- Worktree: `/home/sutanto/JSZ-wt-wave49b`
- Main repo: `/home/sutanto/JSZ`
- Git remote: `origin` → `https://github.com/sutantodadang/JSZ.git`
- No uncommitted work — start fresh from origin/main

## Your Mission
Fix test262 conformance failures in **Temporal** area (do NOT touch areas allocated to other waves):

### Temporal All
- `src/runtime/builtins/temporal/` — all Temporal built-in implementations
- `src/runtime/intl402/Temporal/` — Intl-aware Temporal extensions
- `tests/test262_known_failing.txt` — Temporal entries (filter by `built-ins/Temporal`, `intl402/Temporal`, `staging/Temporal`)
- Focus on:
  - **Temporal.Now** — timeZoneId, instant, plainDateTime, plainDate, plainTime
  - **Temporal.Instant** — from, fromEpochSeconds, fromEpochMilliseconds, toZonedDateTime, since/until rounding
  - **Temporal.PlainDate** — calendarDaysInMonth, daysInMonth, daysInYear, weeksInYear, era/eraYear
  - **Temporal.PlainDateTime** — withPlainTime, withPlainDate, toPlainTime, round
  - **Temporal.PlainTime** — calendarGetter, from, round, until/since
  - **Temporal.PlainYearMonth** — from, add/subtract, until/since
  - **Temporal.PlainMonthDay** — from, with, equals, getCalendar
  - **Temporal.Duration** — round, total, toString, from, add/subtract, with, sign, blank, abs, negated
  - **Temporal.ZonedDateTime** — with, withPlainTime, withPlainDate, round, startOfDay, offset, epochSeconds, offsetNanoseconds, hoursInDay, daysInMonth, daysInYear, weeksInYear, era/eraYear, getISOFields, since/until, equals, toString
  - **Temporal.Calendar** — dateFromFields, yearMonthFromFields, monthDayFromFields, fields, mergeFields, dateAdd, dateUntil, daysInMonth, daysInYear, weeksInYear, monthsInYear, inLeapYear, era/eraYear
  - **Temporal.TimeZone** — getOffsetNanosecondsFor, getOffsetStringFor, getPlainDateTimeFor, getInstantFor, getPossibleInstantsFor, getNextTransition, getPreviousTransition, toString, id
  - **Intl402/Temporal** — Intl.DateTimeFormat integration with Temporal

## Workflow
1. Run `zig build test` to confirm baseline is green
2. Run `zig build conformance-delta` to see current conformance delta
3. Check which Temporal tests are failing: `grep -E "Temporal" tests/test262_known_failing.txt | head -80`
4. Fix files in `src/runtime/builtins/temporal/` and `src/runtime/intl402/Temporal/`
5. After each fix: `zig build test` (must pass 0 failures)
6. Run `zig build differential` (must pass 167/167)
7. Run `zig build conformance-delta` to measure improvement
8. Commit with message: `Wave 49b: <description of fixes>`
9. Push: `git push origin feat/conformance-wave49b`
10. Create PR: `gh pr create --title "Wave 49b: Temporal all" --body "Conformance sweep: Temporal built-ins + Intl402/Temporal"`
11. Merge: `gh pr merge --squash --delete-branch`

## Rules
- **Do NOT modify RegExp/destructuring/language/expressions** files — those belong to Wave 49a
- **Do NOT modify Intl/annexB/eval-code/JSON** files — those belong to Wave 49c
- If you hit an error you cannot fix, stop and commit what you have
- If you hit Claude's turn limit, just stop — the next cycle will continue
- Always verify: `zig build test` (0 failures) + `zig build differential` (167/167)

## Start working!
Begin by running `zig build test` to verify the baseline, then investigate the failing Temporal tests in your area.
